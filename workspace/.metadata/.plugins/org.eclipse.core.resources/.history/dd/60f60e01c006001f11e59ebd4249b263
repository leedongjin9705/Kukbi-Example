package chap14.sec07.exam03;

public class PrintThread extends Thread {
	@Override
	public void run() {
		// Thread.interrupted() 를 사용해서 interrupt() 메소드가 호출되었는지 
		// 확인한 후 while문을 빠져 나가도록 함
		
		while(true) {
			System.out.println("실행중");
		}
	}

}
