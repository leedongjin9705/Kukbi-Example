package chap07.sec06.package1;

public class B {
	// 메소드 선언
	public void method() {
		A a = new A(); // 가능
		a.field = "value"; // 가능
		a.method(); // 가능
		
	}
	/* protected
	 * A 클래스의 protected로 선언된 필드, 생성자, 메소드는 동일한
	 * 패키지에 저장된 클래스들은 모두 접근해서 사용가능
	 * */

}
